# Runtime dependencies. Lower bounds reflect the minimum stable releases this
# alpha is tested against; the upper guard prevents accidental major-version
# upgrades. Bump deliberately when reviewing release notes for security fixes.
Flask>=3.0,<4.0
requests>=2.31,<3.0
cryptography>=42.0,<46.0
urllib3>=2.0,<3.0

# Test dependency, also imported via conftest.
pytest>=8.0,<9.0
